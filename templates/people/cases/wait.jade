- need_resync = people.needResync()
.vmiddle
  if need_resync
    .alert.alert-error 上一次同步数据似乎出错了，重试一下吧
  else
    include ../mods/progress
    p#remains.muted
      正在准备数据，请耐心等待

if need_resync
  hr
  include ../mods/resync

