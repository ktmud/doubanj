- progress_colors = locals.progress_colors || ['success', 'warning']
.progress.progress-striped.active
  each p, i in people.progresses()
    - color = progress_colors[i]
    div(class="progress-bar progress-bar-#{color}", style="width:#{p}%")

